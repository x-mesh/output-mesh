# DESIGN.md

## 원칙

네이티브 macOS 유틸리티의 밀도를 웹에서 재현한다. 장식보다 운영 명료성이 먼저다.

- 시스템 폰트(`-apple-system`), 경로와 해시는 monospace
- 상태는 색만으로 구분하지 않는다. 항상 텍스트 배지를 동반한다 (`최종본`, `원본 없음`)
- 장식성 진입 애니메이션 없음
- 라이트/다크는 기본으로 시스템 설정을 따르고, 상단 바 오른쪽 버튼으로 고정할 수 있다
  (시스템 설정 → 라이트 → 다크 순환, 이 브라우저에만 저장). `public/theme.js` 가 `<head>` 에서
  첫 그림 전에 `data-theme` 을 정하고 CSS 는 그 속성만 본다 — 미디어 쿼리와 속성 선택자에 다크 값을
  두 벌 두면 서로 어긋난다. 렌더한 마크다운은 격리된 문서라 그릴 때의 테마를 박아 넣고, 테마가
  바뀌면 다시 그린다
- 색은 `:root` 토큰으로만 선언하고 나머지는 소비만 한다

## 레이아웃

Xcode 와 같은 2단. 왼쪽 **탐색기**(기본 340px, 240–560px 로 끌어서 조절) / 오른쪽 **본문**.
예전 3열(패싯 / 목록 / 상세)은 셋이 폭을 나눠 가져 어느 것도 주인공이 아니었다. 찾은 뒤에는
내용이 화면을 가진다.

- **상단 바**: 보기 전환, 현황 숫자 네 개, 실시간 표시, 다시 수집. 현황 숫자는 큰 카드가 아니라
  한 줄짜리 버튼이고, 누르면 그 숫자가 가리키는 목록이 된다. 그래서 숫자와 누른 뒤 목록 건수는
  같은 조건(`filterClauses`)으로 센다.
  - 최근 한 시간 안에 수집 오류가 있었으면 실시간 표시 옆에 "수집 오류 · 38분 전"을 글자로 둔다. 누르면
    수집 범위 화면의 "최근 수집 오류"로 간다. 수집은 30초마다 다시 성공하므로 "마지막 수집이 실패했나"만
    보면 한 번 난 실패가 바로 가려진다. 실패한 수집도 화면에 알린다(`failed`).
- **탐색기**: 검색 → 기간 → 접히는 필터 → 트리 → 바닥줄(라이브러리 밖 개수, 수집 범위, 버전).
  - 버전은 바닥줄에 작게 둔다. 버그를 알리거나 새 버전이 받아졌는지 볼 때만 찾는 정보라 상단 바의 조작 사이에 두지 않는다. 기준은 `package.json` 하나이고 릴리스는 `git-kit ship` 이 태그를 단다.
  - **기간**(전체 · 오늘 · 7일 · 30일 · 90일)은 파일 mtime 이 아니라 에이전트가 손댄 시각(출처의
    `occurred_at`)으로 자른다. mtime 은 사람이 고쳐도 바뀐다. "7일"은 오늘을 포함해 이레, 엿새 전
    자정부터다. 시작 시각은 서버가 `PERIOD_DAYS` 하나로 정해서 트리 · 패싯 · 개요 · 현황 숫자 ·
    활동 보기가 같은 자정을 쓴다. 현황의 "최근 7일"은 이 기간 버튼과 같은 것이다.
  - 기간은 필터 토큰으로 보이지 않는다. 보는 창이라 기간 줄 자체가 상태를 보여준다.
  - 트리는 저장소 → 폴더 → 파일. 작업공간이 없는 Aside 산출물은 수집기 → 작업 제목 → 파일.
    저장소 밖 경로는 맨 아래 한 묶음이다.
  - git worktree 와 저장소 하위 폴더에서 돈 세션은 본 저장소로 접는다. 실측에서 작업공간 44곳 중 18곳이
    worktree 라 term-mesh 하나가 트리와 분포에서 네 군데로 갈라져 있었다(접은 뒤 22곳). worktree 의
    파일은 저장소 아래 `⑂ 이름` 한 단에 선다 — 본 저장소와 worktree 에 같은 README 가 있어도 구분된다.
    위치 줄에도 같은 이름표가 붙는다. 작업공간 패싯 · 필터 · 활동 · 그래프가 모두 본 저장소 기준이다.
  - 파일 없이 폴더 하나만 품은 폴더는 `xm/skills` 처럼 한 줄로 접는다(VS Code compact folders).
    파일 하나만 든 끝 폴더는 `review/SKILL.md` 처럼 파일 줄에 폴더 이름을 붙인다.
  - 묶음은 최근 순, 파일은 최종본 먼저 그다음 최근 순.
  - **묶기** 선택으로 첫 단을 바꾼다: 저장소(기본) · 에이전트 · 앱 · 날짜 · 종류. 저장소가 아닌 방식은
    둘째 단이 위치(저장소 · 저장소 밖 · Aside 작업 제목)이고, 파일 줄 앞에 저장소 안 마지막 폴더를 붙인다.
  - 여러 에이전트·앱이 손댄 파일은 각 묶음에 모두 나온다. 필터가 `EXISTS` 로 그렇게 답하므로 묶음도
    같은 말을 한다. 그래서 묶음 개수의 합이 전체보다 클 수 있다. 앱 목록은 대표 출처 하나가 아니라
    모든 출처에서 온다(`collectors`).
  - 날짜는 에이전트가 마지막으로 손댄 시각(기록이 없으면 파일이 바뀐 시각)을 보는 사람의 자정 기준으로
    나눈다: 오늘 · 어제 · 지난 7일 · 지난 30일 · 월별. 기간 선택과 같은 시각이다.
  - 펼침 상태는 묶기 방식마다 따로 남는다.
  - 펼친 상태·필터 패널·탐색기 폭은 이 브라우저의 `localStorage` 에만 남는다. 검색 중에는
    맞는 것을 모두 펼치고, 그 동안의 접기는 저장하지 않는다.
  - 키보드: ↑↓ 이동(파일이면 바로 연다), → 펼치기, ← 접기·부모로, `/` 검색, 검색창에서 ↓ 트리로.
  - **검색어가 있으면 트리 대신 관련도순 한 줄 목록이다.** 저장소별로 묶으면 가장 잘 맞은 파일이
    최근에 손댄 저장소 아래로 흩어진다. 줄마다 트리가 말해 주던 위치와 본문에서 처음 맞은 대목을
    붙이고, 이름 · 부제 · 발췌에서 맞은 곳을 칠한다. 이름에만 맞았으면 발췌는 비운다.
    순서는 최종본 먼저, 그다음 전문 검색은 bm25(이름 10 · 작업 제목 4 · 경로 2 · 본문 1), 부분 일치는
    맞은 곳(이름 → 작업 제목 → 경로 → 본문)이다. 필터와 기간만 걸렸을 때는 관련도가 없으므로 트리
    그대로다. 묶기와 모두 접기는 검색 중에 숨기고, 그 자리의 건수 옆에 "관련도순"이라고 적는다.
- **활동 보기**는 고른 파일이 없으면 화면 전체를 쓴다. 340px 칸에서는 세션마다 파일 칩 열댓 개가 여러 줄로
  접혀 한 화면에 세션 서너 개만 보였고, 오른쪽에는 활동과 무관한 직전 문서가 남아 있었다. 넓을 때는 누가 ·
  언제 · 어디서를 왼쪽 기둥에, 시킨 일과 쓴 파일을 오른쪽에 둔다(한 화면에 여덟 개). 같은 목록이 폭만 달라진다 —
  파일 칩을 누르면 타임라인은 왼쪽 칸으로 물러나고 오른쪽에 내용이 서며, 제목(개요)을 누르면 다시 넓어진다.
  활동으로 들어갈 때는 보던 문서를 내려놓는다.
- **본문**: 머리(이름·부제·에이전트·위치·동작) 아래 미리보기가 남는 높이를 전부 쓴다. 정보 패널은
  본문 폭이 820px 이상이면 오른쪽 300px 인스펙터, 그보다 좁으면 미리보기 아래로 내려간다.
  아무것도 고르지 않으면 **개요**를 보여준다(아래).

## 언어

화면은 영어와 한국어. 기본은 브라우저 언어(`ko` 로 시작하면 한국어, 아니면 영어)이고 상단 바의 `KO` / `EN` 으로 바꾸면 이 브라우저에 남는다(`aoc.lang`).

- 문구는 `public/i18n.js` 사전 하나에만 있다. `app.js` 는 `t(key, params)` 로만 부르고, `index.html` 의 고정 문구는 `data-i18n` / `data-i18n-attr` 로 단다.
- 개수가 들어가는 문구는 매개변수를 받는 메시지다. 한국어는 "3개" 하나지만 영어는 "1 item / 3 items" 로 갈린다.
- 날짜·상대 시각·숫자는 `Intl` 에 현재 언어를 넘겨 만든다("3분 전 / 3 minutes ago", "어제 / yesterday").
- 서버는 종류 값(`text`, `markup` …)만 보내고 이름표는 화면이 붙인다. 번역된 글자를 키로 쓰지 않는다.
- `i18n.js` 는 `theme.js` 처럼 동기로 돌아 첫 그림 전에 정적 문구를 바꾼다. 영어를 고른 사람에게 한국어가 번쩍이지 않게.
- 언어를 바꾸면 트리 묶음 이름, 필터, 개요, 열어 둔 상세를 새 언어로 다시 그린다.
- 로그에서 찾는 한국어(`describe.mjs` 의 "계속 진행해" 같은 이어가기 말)는 화면 문구가 아니라 데이터라 번역하지 않는다.
- CLI 출력(스피너, 도움말)은 아직 한국어만이다.

## 개요

상단 바의 제목을 누르면 언제든 돌아온다. 주소는 `#/`(개요), `#/a/<id>`(파일), `#/coverage` 이고
브라우저 뒤로 가기가 개요와 파일 사이를 오간다. 파일에서 파일로 옮길 때는 기록을 쌓지 않는다 —
화살표로 스무 개를 지나면 뒤로 가기를 스무 번 눌러야 개요로 돌아오게 된다.

개요는 탐색기와 같은 검색어·필터로 센다. 검색하면 그 결과의 개요가 된다.

위에서 아래로 목록 한 줄(방금 일어난 일을 넓게, 최종본을 좁게), 그다음 그래프 두 줄(활동 · 분포) 순이다. 한눈에 보는 것과 하나씩 읽는 것을 섞지 않는다. 예전에는 그래프가 먼저여서 첫 화면(높이 929px)의 4분의 3을 몇 달 치 추이가 차지하고 목록은 다섯 줄만 보였다. 이 화면을 여는 두 순간(찾을 때, 흘끗 볼 때) 모두 방금 바뀐 것과 최종본이 먼저 필요하다. "최근 바뀐 것" 목록은 뺐다 — 방금 일어난 일이 같은 파일을 무엇이 · 누가 · 언제까지 더 말한다. 개요 폭이 900px 보다 좁으면 두 목록을 위아래로 쌓는다.

- **활동 그래프**: 칸마다 에이전트별로 쌓은 막대. 칸은 기간을 따른다 — 오늘은 시간, 7 · 30 · 90일은
  하루, 전체는 한 주(첫 기록이 든 주의 월요일부터, 최대 104주). 파일의 mtime 이 아니라 출처의
  `occurred_at`(에이전트가 손댄 시각)을 센다. 에이전트 · 앱 · 작업공간 필터가 있으면 그 출처의 기록만
  센다 — 파일 집합만 좁히면 같은 파일을 만진 다른 에이전트의 날까지 섞인다. 칸은 SQL 이 아니라 JS 에서
  자른다(SQLite `localtime` 과 프로세스 TZ 가 다를 수 있다).
- **분포**: 종류 · 에이전트 · 작업공간. 사이드바 패싯과 같은 숫자라 누르면 같은 필터가 된다.
- **최종본** 목록.

- **방금 일어난 일**(머리 바로 아래, 목록의 첫 칸): 생김 · 바뀜 · 옮김 · 사라짐 · 돌아옴. 에이전트 기록으로 본 변화와 디스크에서 발견한 변화를 구분한다. "에이전트 밖"이라 부르지 않는다 — 에이전트가 셸로 쓴 파일도 디스크로 잡힌다. 트리의 해당 파일에는 10분 동안 "새로 / 바뀜" 표시가 붙는다.
- 열은 목록이 정하고 줄은 subgrid 로 따른다. 줄마다 따로 그리드를 두면 칩 너비에 따라 위치 칸이 줄마다 어긋난다.
- 제 칸 안에서 여덟 줄 반 높이로 스크롤한다. 반 줄이 걸쳐 있어야 아래에 더 있다는 게 보인다. 끝에 닿으면 이전 기록을 30줄씩 받는다(사건 번호 `before` 커서). 보관이 30일이라 수천 줄이 될 수 있어 한 번에 받지 않는다.
- 실시간 갱신은 가장 새 줄 뒤(`after`)만 받아 위에 붙인다. 첫 쪽을 다시 받으면 불러 둔 이전 기록이 사라진다. 스크롤을 내린 채였으면 붙은 줄 높이만큼 내려 보던 줄을 제자리에 둔다.
- 피드 행마다 에이전트를 앱 단위(Codex · Claude Code · Aside)로 단다. 에이전트 기록으로 본 변경은 그 에이전트를 칠한 칩으로, 디스크에서 본 변경은 파일을 만든 에이전트를 점선 칩으로 단다 — 누가 바꿨는지는 모른다(Codex 가 만든 설정 파일을 앱이 스스로 고친 경우가 있다). 에이전트 출처가 없는 저장소 문서는 칩 없이 "디스크에서 발견"만 둔다.
- 피드도 라이브러리처럼 코드·기타를 뺀다. 대신 뺀 건수를 한 줄로 남기고("라이브러리가 숨긴 변경: 코드 3개"), 누르면 그 종류로 거른다. 코드를 고쳤는데 피드가 조용하면 수집이 멈춘 것처럼 보인다.

- **위젯 열 개 중에서 골라 세운다.** 방금 일어난 일 · 최종본 · 활동 그래프 · 종류 · 에이전트 · 작업공간 · 최근 작업 · 수집 상태 · 즐겨찾기 · 태그. 제목줄과 키보드 안내는 틀이라 대상이 아니다. 처음 켜 두는 것은 앞의 여섯 개다 — 열 개를 다 펼치면 첫 화면이 읽히지 않는다. 고른 구성은 이 브라우저의 `localStorage`(`aoc.home.blocks`)에만 남고, 저장된 구성과 기본값을 합칠 때 모르는 id 는 버리고 빠진 id 는 뒤에 붙인다 — 위젯이 늘어도 설정이 안 깨진다.
- **분포는 차원마다 위젯 하나다.** 한 덩어리로 묶여 있으면 작업공간만 키우거나 종류만 뺄 수 없다. 넷 다 사이드바 패싯과 같은 숫자라 누르면 같은 필터가 된다.
- **켜 둔 위젯이 쓰는 것만 받는다.** 최근 작업은 `/api/activity`, 수집 상태는 `/api/status` 가 더 필요하다. 꺼져 있으면 부르지 않는다 — 안 보이는 위젯 때문에 30초마다 요청이 늘지 않게. 즐겨찾기는 이미 받아 둔 목록에서 거르고, 태그와 분포는 패싯에서 온다.
- **"최근 오류"는 한 자리에서만 정의한다.** 수집 상태 위젯도 상단 표시와 같은 `recentError`(한 시간)를 쓰고, 그보다 오래된 기록은 건수만 조용히 남긴다. 두 자리가 다른 기준으로 "최근"을 말하면 숫자를 믿을 수 없다.
- **자리와 크기는 12칸 격자에서 끌어 정한다**(`gridstack` 13.3.0, `public/vendor/`). 손잡이를 끌면 옮기고, 오른쪽 아래 모서리를 끌면 크기가 바뀐다. 고른 배치(`x, y, w, h`)는 `aoc.home.layout` 에 남는다. 기본값은 3안의 모습 그대로다 — 목록 둘이 2:1 로 한 줄, 그 아래 그래프와 분포.
- **격자는 자리만 맡고 카드는 그리지 않는다.** `PRODUCT.md` 의 안티레퍼런스는 "둥근 카드 격자"라는 생김새다. 쉴 때 블록에는 테두리도 그림자도 없고, 경계는 끌거나 크기를 바꾸는 동안에만 보인다. `animate: false` 로 장식성 움직임도 끈다.
- **손잡이 둘 다 늘 보인다.** `alwaysShowResizeHandle` 을 켠다. 기본값은 hover 때만 보이는데, 그러면 바꿀 수 있다는 걸 알 수 없다 — 작은 `구성` 링크 하나로는 아무도 못 찾았다는 것이 이 화면에서 이미 확인됐다.
- **끌기는 손잡이로만 건다**(`handle: '.drag-handle'`). 위젯 전체를 잡게 두면 피드 글자를 긁어 고를 수 없다.
- **수집 알림에 그리드째 다시 세우지 않는다.** 30초마다 오는 알림에 다시 만들면 화면이 깜빡이고 끌던 손이 끊긴다. 틀은 그대로 두고 `fillBlocks` 가 내용만 갈아 끼우며, 블록을 켜고 끄거나 배치를 되돌릴 때만 다시 세운다.
- **크기를 바꾸면 그래프는 스스로 다시 그린다.** 활동 그래프의 `ResizeObserver` 가 폭 변화를 이미 보고 있다.
- **키보드 경로는 구성 패널이다.** 끌기와 크기 조절은 마우스만 닿는다. 패널에서 켜고 끄고, ↑↓ 로 한 칸씩 옮기고, 기본 배치로 되돌린다. 세밀한 크기까지 키보드로 열면 격자 편집기를 하나 더 만드는 일이 된다.
- **구성 패널은 겹쳐 띄운다.** 제목줄 안에서 펼치면 개요 전체가 150px 밀린다. 조작은 잠깐이고 아래 내용이 자리를 지켜야 한다.
- 넷을 다 끄면 개요가 백지가 되므로 그때는 안내 한 줄과 기본 구성 복원 버튼을 둔다.

현황 숫자는 상단 바에 있으므로 개요에 큰 숫자 카드를 두지 않는다.

**탭 줄은 이 창이 백그라운드일 때 보이는 유일한 자리다.** 인페이지 토스트는 거기서 보이지 않고 돌아오면 이미 닫혀 있다(측정: 변경이 분당 1.9건이라 8시간이면 약 900건). 그래서 알림은 `document.title` 에만 적는다 — 안 본 변경 수 `(3) Output Mesh`, 수집이 멎으면 `⚠`, 둘 다면 `⚠ (3) Output Mesh`.

- **수는 서버가 준 원본이다.** 라이브러리가 숨기는 종류(코드 · 메모 · 기타)도 든다. 돌아왔을 때 피드의 "라이브러리가 숨긴 변경" 줄과 더하면 맞춰 볼 수 있고, 활동 보기처럼 피드를 받지 않는 화면에서도 센다. 99 를 넘으면 `99+`.
- **숨겨진 동안만 센다.** 보고 있으면 피드가 이미 개요 맨 위에서 같은 것을 말한다. 포커스가 아니라 가시성(`visibilitychange`)으로 판단한다 — 창을 나란히 띄워 두면 포커스가 없어도 보고 있는 것이다.
- **멈춤 판정은 상단 표시와 같은 조건이다.** 화면과 탭이 다른 말을 하지 않게.
- 브라우저 자체가 다른 앱에 가려지면 탭 줄도 안 보인다. 거기까지는 이 방법으로 안 된다.

## 변경 기록

- 기록은 `ingestFile`(생김 · 바뀜 · 옮김 · 돌아옴)과 `markMissing`(사라짐) 두 곳에서만 남긴다. 부르는 곳(스윕, 세션 로그, 가져오기, 다시 확인)마다 남기면 규칙이 갈라진다.
- 내용이 그대로인 `touched` 는 기록하지 않는다. 30초마다 모든 파일이 이 길을 지난다.
- 빈 카탈로그의 첫 수집은 조용히 한다(`quiet`). 수천 개가 "새로 생김"으로 피드를 덮지 않게.
- 에이전트 기록으로 본 변경은 알아챈 때가 아니라 로그가 말하는 그때로 적는다. 그때가 네 시간(`FEED_BACKLOG_S`)보다 전이면 피드에 남기지 않는다. 서버가 꺼져 있던 동안 밀린 로그가 재시작 때 몇 주 전 파일을 "방금"으로 올렸다. 파일과 출처는 그대로 들어가므로 라이브러리와 활동 보기에는 실제 시각으로 보인다. 세션이 없는 출처와 Aside 폴더 스윕은 그때를 모르므로 알아챈 때로 남는다.
- 세션 로그로 찾은 파일은 30초마다 다시 확인한다(`recheckKnownFiles`). 크기나 수정 시각이 바뀐 것만 해시한다. Aside 출처 파일은 스윕이 대조하므로 뺀다 — 둘이 같이 판정하면 수집 범위 밖으로 옮겨진 파일을 사라짐 · 돌아옴으로 번갈아 적는다.
- 에이전트가 일한 저장소는 폴더 감시를 건다. 처음에는 최근 7일 문서를 조용히 채우고(원래 있던 문서를 알게 된 것이지 방금 일어난 일이 아니다), 그 뒤로 생기거나 바뀐 문서는 몇 초 안에 "디스크에서 발견"으로 보인다.
- 그래서 지연이 다르다. 에이전트가 도구로 쓴 파일과 저장소 문서는 몇 초 안에, 그 밖에서 고친 파일은 다음 주기 수집(최대 30초)에 보인다.
- 워처는 SSE 로 새 변경 개수만 알리고, 화면은 자기 필터로 목록을 다시 받는다. 개수는 번호 차이가 아니라 행을 세어 낸다 — 오래된 기록(30일)을 지우면 번호가 빈다.

### 그래프 규칙 (dataviz)

- 막대는 18px 이하, 위 끝만 4px 둥글게, 쌓인 조각 사이는 선이 아니라 2px 바탕색 틈.
- 격자와 축은 1px 실선, 흐리게. 눈금은 1·2·5 계단.
- 범례에 에이전트별 합계를 단다. 툴팁은 날짜 칸 전체가 겨냥 대상이고 모든 에이전트 값을 보여준다.
  같은 숫자를 `표로 보기`로도 읽을 수 있다. 키보드는 그래프에 포커스를 두고 ←→.
- 에이전트 색은 개체를 따른다(`AGENT_ORDER` 고정 순서, 필터로 줄어도 색이 그대로). 검증기 결과:
  라이트 `#00785a / #d97757 / #7c5cff` 적색맹 인접 ΔE 8.3, 다크 `#00785a / #d4744e / #8a6cff`
  ΔE 7.5(6–8 구간이라 범례·틈·표 보기를 함께 둔다). 에이전트가 아닌 분포는 한 계열이라 한 색(`--bar`).
- 글자는 계열 색을 입지 않는다. 색은 옆의 점·견본·짧은 선이 맡는다.

선택 강조는 Finder 를 따른다. 트리에 포커스가 있으면 강조색, 다른 곳을 보는 동안에는 회색이다.

검색창 오른쪽 힌트가 지금 어느 경로인지 알려준다: `전문 검색`(trigram) 또는 `부분 일치`(LIKE).
한글 2글자 질의가 조용히 0건을 반환하던 문제를 사용자가 인지할 수 있게 하는 장치다.

마크다운의 프런트매터는 `marked` 가 구분선과 큰 제목으로 뭉개므로 떼어서 문서 위 작은 키-값 표로
그린다.

색 막대(`border-left` 강조)는 쓰지 않는다. 공급자는 칩 앞의 작은 점과 글자로, 상태는 글자 배지로 말한다.

## 미리보기 자세

에이전트가 만든 HTML은 신뢰할 수 없는 문서로 다룬다.

- `<iframe sandbox>`에서 렌더하고 **`allow-same-origin`을 주지 않는다**. 불투명 오리진이라
  카탈로그 API나 다른 아티팩트에 닿지 못한다
- 기본은 스크립트 차단. 아티팩트별 토글로만 허용하고 그 선택은 DB에 남는다
- 허용해도 `connect-src 'none'`은 유지된다. 로컬 전용이라는 약속은 협상 대상이 아니다
- SVG는 `<img>`로 렌더한다. `<iframe>`이 아니라서 내장 스크립트가 실행되지 않는다
- xlsx 는 서버가 `unzip` 으로 시트 XML 을 읽어 값만 표로 보낸다(시트마다 처음 200행 · 30열, XML 은 앞 2 MB). 화면은 텍스트 노드로만 그린다. 서식·병합·차트는 없고 수식은 마지막 계산값이다. 잘렸다는 표시는 `<dimension>` 이 아니라 실제로 멈췄을 때만 한다 — 빈 서식 행까지 범위에 넣는 도구가 있다

### V7 수동 확인 (1회, 기록용)

`rack-3d-sketch.html`을 `allow_scripts = 0`으로 열고 네트워크 모니터를 붙여
외부 요청 0건과 `file://` 자가 이동이 차단되는지 확인한다. 자동화된 부분은 `test/server.test.mjs`가 덮는다.

## 문구

- "원 작업 링크"가 아니라 **"원 작업 정보"**. Aside에는 특정 세션을 여는 URL 스킴도 CLI 서브커맨드도 없다.
  못 하는 점프를 UI가 약속하지 않는다.
- 전문 검색이 불가능한 형식은 상세에 그 사실을 적는다. 검색이 조용히 덜 반환하게 두지 않는다.
- 에이전트가 자기용으로 적는 메모(Claude Code 의 프로젝트 memory)는 종류 "에이전트 메모"로 갈라 코드처럼 라이브러리에서 숨긴다. 종류 필터로 꺼내 보고 검색도 된다. 피드의 "라이브러리가 숨긴 변경"에도 같은 이름으로 센다.
- 항상 비어 있을 필터는 만들지 않는다. Aside는 workspace를 주지 않으므로 그 패싯은 없다.
