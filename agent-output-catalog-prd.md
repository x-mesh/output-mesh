# PRD: Agent Output Catalog

## 1. 제품 요약

여러 AI 에이전트가 만든 결과물을 한곳에 모으고, 검색·미리보기·출처 추적·여러 머신 동기화를 제공하는 로컬 우선 데스크톱 앱입니다.

작업명은 **Agent Output Catalog**으로 둡니다. Aside, Codex, Claude 등 기존 도구를 대체하거나 실행하지 않고, 각 도구가 만든 최종 산출물을 읽기 전용으로 수집합니다.

## 2. 문제

AI 에이전트가 만든 보고서, PDF, 스프레드시트, HTML 도구, 이미지, 코드 생성물은 각 세션·폴더·서비스에 흩어집니다.

사용자는 다음을 빠르게 알기 어렵습니다.

- 이 파일을 어느 에이전트가 어느 작업에서 만들었는가
- 비슷한 결과물이 이미 있는가
- 이것이 최종본인가, 임시 파일인가
- 다른 Mac에서 만든 결과물을 현재 머신에서 볼 수 있는가
- 원래 대화나 작업으로 돌아갈 수 있는가

## 3. 목표

1. 여러 에이전트의 산출물을 한 라이브러리에서 찾습니다.
2. 파일마다 생성 도구와 원래 작업의 출처를 보존합니다.
3. 콘텐츠 해시 기반으로 중복과 버전을 구분합니다.
4. 사용자가 소유한 여러 머신에서 결과물을 동기화합니다.
5. 원본 폴더와 에이전트 데이터에 쓰지 않습니다.

## 4. 비목표

- 에이전트 실행, 세션 관리, 오케스트레이션
- 모든 파일 시스템 파일의 무차별 수집
- 프롬프트와 전체 transcript의 기본 수집·동기화
- 팀 권한, 공유 링크, 중앙 SaaS
- 자체 클라우드 동기화 서버
- 에이전트별 아티팩트 UI를 대체하는 기능

## 5. 대상 사용자

여러 AI 에이전트를 사용하고, 여러 머신에서 개발·조사·문서 작업을 수행하는 개인 사용자입니다.

주요 사용 시나리오는 다음과 같습니다.

1. Aside가 만든 보고서와 PDF를 자동으로 발견합니다.
2. Codex가 생성한 결과물을 manifest로 등록합니다.
3. Claude Artifact의 URL 또는 내려받은 파일을 가져옵니다.
4. 다른 Mac에서 만든 결과물을 검색하고 미리봅니다.
5. 결과물에서 원래 작업, 저장소, 생성 에이전트로 돌아갑니다.

## 6. 핵심 개념

### Artifact

사용자가 보관할 가치가 있다고 판단한 파일 또는 URL 기반 산출물입니다.

예: PDF 보고서, HTML 대시보드, XLSX, 이미지, Markdown 문서, 생성 코드, Claude Artifact 링크.

### Provenance

산출물이 어디서 왔는지 설명하는 메타데이터입니다.

```text
provider: aside | codex | claude | import
session_ref: 원래 작업 또는 세션 식별자
workspace: 저장소 또는 작업 폴더
source_machine: 생성한 머신
source_path: 원본 파일 경로
created_at: 발견 또는 생성 시각
content_hash: 파일 내용 해시
state: discovered | final | archived
```

### Version lineage

같은 결과물의 변경본을 하나의 계열로 묶는 정보입니다. 해시가 다르면 별도 버전이며, 사용자가 같은 산출물 계열로 연결할 수 있습니다.

## 7. 기능 요구사항

### 7.1 수집기

| 우선순위 | 공급자 | 수집 방식 |
|---|---|---|
| P0 | Aside | 작업별 artifact 폴더를 읽기 전용 감시 |
| P0 | Generic import | 사용자가 폴더 또는 파일을 직접 등록 |
| P1 | Codex | `.agent-artifacts/manifest.json`에 명시된 결과물 수집 |
| P1 | Claude | Artifact URL 및 다운로드 파일 가져오기 |
| P2 | Cursor, Copilot 등 | 공급자별 adapter 또는 manifest 방식 |

수집기는 다음을 만족해야 합니다.

- 원본 폴더에 파일을 쓰거나 이름을 바꾸지 않습니다.
- 파일 변경을 감지하면 해시와 메타데이터를 갱신합니다.
- 임시 파일은 기본적으로 숨기고, 사용자가 표시할 수 있습니다.
- 수집 실패는 원인과 원본 경로를 보여줍니다.

### 7.2 라이브러리

사용자는 다음 기준으로 탐색·필터할 수 있어야 합니다.

- 파일 형식
- 에이전트와 공급자
- 프로젝트 또는 작업공간
- 생성 머신
- 기간
- 상태: 발견됨, 최종본, 보관됨
- 태그와 즐겨찾기

지원 기능:

- 파일명·텍스트·메타데이터 검색
- 지원 형식의 즉시 미리보기
- 원본 파일 열기
- 원래 작업 또는 Artifact URL 열기
- 태그, 메모, 최종본 표시
- 중복 파일 및 버전 계열 보기

### 7.3 동기화

동기화는 앱이 관리하는 별도 artifact store에서 동작합니다.

```text
artifact-store/
  blobs/<content-hash>
  manifests/<artifact-id>.json
  machines/<machine-id>.json
```

- 파일은 콘텐츠 해시 기준 immutable blob으로 저장합니다.
- 메타데이터는 머신별 파일로 기록해 동기화 충돌을 줄입니다.
- 초기 버전은 Syncthing 호환 폴더를 사용합니다.
- iCloud Drive, Dropbox 등 일반 파일 동기화 경로도 선택할 수 있습니다.
- 프롬프트와 transcript는 기본적으로 동기화하지 않습니다.
- 충돌이 나면 파일을 덮어쓰지 않고 두 상태를 보존합니다.

### 7.4 개인정보 보호

- 기본 동작은 로컬 전용입니다.
- 원본 파일과 artifact store 밖의 파일을 업로드하지 않습니다.
- Claude Artifact URL처럼 외부 서비스 링크를 등록할 때는 링크와 최소 메타데이터만 저장합니다.
- 민감 파일은 태그 또는 경로 규칙으로 수집 제외할 수 있어야 합니다.

## 8. 화면

1. **Library**
   - 전체 결과물 목록
   - 검색, 필터, 정렬
   - 파일 형식별 미리보기

2. **Artifact Detail**
   - 미리보기
   - provenance
   - 버전 계열
   - 태그와 상태
   - 원 작업, 원본 경로, 외부 Artifact 링크

3. **Sources**
   - Aside, Codex, Claude, Import folder 상태
   - 마지막 수집 시간과 오류
   - 수집 제외 규칙

4. **Sync**
   - 연결된 머신
   - 마지막 동기화 상태
   - 충돌 및 미동기화 항목

## 9. MVP

MVP에는 다음만 포함합니다.

- Aside 자동 수집
- 폴더·파일 직접 가져오기
- SQLite 기반 로컬 인덱스
- 파일명·전문·메타데이터 검색
- PDF, 이미지, HTML, 텍스트 미리보기
- 태그, 즐겨찾기, 최종본 표시
- 원본 경로 및 원 작업 링크
- Syncthing 호환 artifact store
- 생성 머신 필터

Codex manifest와 Claude URL 가져오기는 MVP 직후 추가합니다.

## 10. 성공 기준

- Aside 산출물이 생성 후 1분 이내 라이브러리에 보입니다.
- 사용자가 30초 안에 과거 결과물을 찾아 열 수 있습니다.
- 동일 파일은 여러 머신에서 한 번만 저장됩니다.
- 원래 작업 또는 원본 파일로 되돌아갈 수 있습니다.
- 동기화 충돌로 원본 또는 메타데이터가 사라지지 않습니다.

## 11. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| 공급자별 저장 구조 변경 | adapter를 분리하고 수집 실패를 명시 |
| 임시 파일로 라이브러리 오염 | manifest, 제외 규칙, 최종본 상태 사용 |
| 프롬프트·transcript의 민감성 | 기본 미수집, 명시적 연결만 허용 |
| 파일 동기화 충돌 | immutable blob과 머신별 manifest 사용 |
| Fast.io 같은 중앙 스토리지와의 겹침 | 기존 도구 결과를 사후 수집하는 개인 레지스트리에 집중 |

## 12. 미결정 사항

- `final` 상태를 사용자가 직접 지정할지, 에이전트 manifest로 자동 지정할지
- Codex용 manifest를 공용 Agent Artifact Manifest 규격으로 확장할지
- Claude Artifact URL의 메타데이터를 어디까지 자동 수집할지
- Syncthing 외에 기본 동기화 방식을 제공할지
